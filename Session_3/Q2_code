class Solution:
	# @param A : tuple of integers
	# @return an integer
	def singleNumber(self, A):
        d={}
        for i in A:
            if i not in d.keys():
                d[i]=0
            d[i]+=1
        for i in d.keys():
            if d[i]==1:
                return i
